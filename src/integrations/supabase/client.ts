/**
 * Re-export the Supabase client from the lib directory
 * This file exists to maintain compatibility with existing imports in the codebase
 */
import { supabase } from "@/lib/supabase";

export { supabase }; 